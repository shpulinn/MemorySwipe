import 'package:flutter/material.dart';

class PhotoSwipeScreen extends StatelessWidget {
  const PhotoSwipeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Photo Swipe'),
      ),
    );
  }
}